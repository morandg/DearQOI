name: Push workflow
on: push
jobs:
  say-hello:
    uses: ./.github/workflows/hello.yml
    with:
      who: "Albert"
      planet: "Mushroom"

  build:
    name: Build Da funk
    runs-on: ubuntu-22.04

    steps:
      - name: build
        run: echo "building ..."
